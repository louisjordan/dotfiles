# config

:checkered_flag: speedy unix installs

# install

```
git clone git@github.com:louisjordan/config.git ~/git/config
~/git/config/install.sh
```

## todo

- [ ] install packages
  - [ ] tmux, node, rust, python, mplayer
- [ ] add binaries to /usr/local/bin
  - [ ] ~/path/to/binary "$@" # passes through all arguments
  - [ ] warn if same name files already exist
